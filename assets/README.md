Coloque aqui o arquivo PNG do logo do SENAC

- Nome recomendado: `Senac.png` (respeite caixa alta/minuscula se o servidor for case-sensitive).
- Caminho esperado pelas páginas: `/assets/Senac.png`

Se você preferir outro nome (por exemplo `senac-logo.png`), atualize as tags `<link rel="icon">` e as meta tags `og:image`/`twitter:image` nas páginas para apontarem para o novo nome.

Observação: o repositório já contém `Senac.png` (verifique `assets/`); se você quiser que eu renomeie o arquivo para `senac-logo.png`, eu posso fazer isso e atualizar todos os HTML.
